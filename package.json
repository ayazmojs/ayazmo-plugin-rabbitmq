{
  "name": "ayazmo-plugin-rabbitmq",
  "version": "0.1.0",
  "description": "ayazmo plugin rabbitmq",
  "exports": "./dist/index.js",
  "author": "ayazmo.io",
  "license": "MIT",
  "rootDir": "src",
  "type": "module",
  "sideEffects": false,
  "keywords": [
    "ayazmo-plugin-rabbitmq"
  ],
  "files": [
    "dist"
  ],
  "dependencies": {
    "@ayazmo/core": "^0.2.7",
    "@ayazmo/types": "^0.2.7",
    "@ayazmo/utils": "^0.2.7",
    "@changesets/changelog-github": "^0.5.0",
    "@changesets/cli": "^2.27.7",
    "amqplib": "^0.10.4",
    "rascal": "^20.1.0"
  },
  "scripts": {
    "build": "rimraf dist && tsc --build",
    "watch": "tsc --watch",
    "version": "changeset version",
    "publish": "changeset publish",
    "test": "echo \"Error: no test specified\" && exit 0"
  },
  "devDependencies": {
    "rimraf": "^5.0.5",
    "typescript": "^5.4.2"
  }
}
